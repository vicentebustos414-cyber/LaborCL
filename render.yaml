services:
  - type: web
    name: laborcl
    env: node
    buildCommand: npm install && npm run build:react
    startCommand: node server.js
    envVars:
      - key: GROQ_API_KEY
        sync: false
